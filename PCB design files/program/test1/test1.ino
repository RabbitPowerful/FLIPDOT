void setup() {
    PINMODE(, OUTPUT);
    PINMODE(, OUTPUT);
    PINMODE(, OUTPUT);
    PINMODE(, OUTPUT);
}

void loop() {
  // We have:
  // Row / Col set for ON
  // Row / Col set for OFF
  // I will have to try to simulate the circuit in such a manner that the off portion will be handled by itself kind of.
  
}
